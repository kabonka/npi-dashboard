# 2026-04-22

## NPI Check 整理版 - 新增甘特图
- 用户要求在 NPI check_整理版.xlsx 中添加甘特图（Gantt Chart）
- 从整理版数据（20 个 Model Name，49 条记录）中提取每个 Model 的最早 MP 行的各 Stage 日期
- 使用 matplotlib 生成甘特图，包含：Kickoff/DVT/EVT/MVT/BTO/ATS/MP 各阶段时间线、Today 标记线（红色虚线）、Stage 标签和颜色编码
- 甘特图作为独立 Sheet「Gantt Chart」嵌入 Excel，排在 NPI Check Sheet 之前
- 甘特图生成脚本：gen_gantt.py（已清理）
- 安装了 matplotlib 和 numpy（系统之前没有）
- 最终文件：c:\Users\msipm\Desktop\work\NPI check_整理版.xlsx（225.8 KB）
- 用户反馈看不到甘特图，重新生成了高清 PNG + 嵌入 Excel
- 新增文件：c:\Users\msipm\Desktop\work\NPI_Gantt_Chart.png（219.5 KB，4461x2962 px）
- 用户要求修改：X轴用月份、去掉BTO、甘特图嵌入NPI Check Sheet的Highlight列之后
- 甘特图只画6个阶段（Kickoff/DVT/EVT/MVT/ATS/MP），不画BTO ready
- 图片锚定在P2位置（Highlight O列之后），尺寸1535x1104px
- 删除了独立的Gantt Chart Sheet，只保留NPI Check一个Sheet
- 用户第三次要求：不要独立图片，直接用单元格填充在 NPI Check Sheet 画甘特图
- P列起每列代表一个月份（2024-11 ~ 2026-09，共23列，P~AL）
- 用单元格背景色填充表示各Stage时间段，字母标记（K=Kickoff/D=DVT/E=EVT/M=MVT/A=ATS/P=MP）
- Today（2026-04）列标红色★，表头浅红底色
- 冻结窗格设为 P2（左右滚动时前15列固定）
- 不再生成独立Gantt Chart Sheet或PNG图片
- 已删除旧的NPI_Gantt_Chart.png
- 用户第四次要求：甘特图与每行 Model 对齐，跟着每行数据画
- 修正了 na 日期的处理逻辑：当某阶段日期为 na 时，直接跳到下一个有效阶段
- 每行甘特图与对应 Model 行完全对齐，从 P 列开始每列一个月份
- 使用字母标记+背景色：K(DVT蓝)/D(紫)/E(橙)/M(红)/A(绿)/P(粉)
- 用户第五次修改：月份表头只显示月份数字(1-12)，年份合并显示在第一行
- MVT用小写"m"，MP用大写"M"，其余阶段用首字母
- 表头改为两行：Row1=年份(合并单元格)，Row2=月份(1-12)
- 冻结窗格改为P3（前15列+前2行表头固定）
- 用户第六次修改：Stage按实际时间段画
  - Kickoff日期~DVT-start = Kickoff阶段
  - DVT-start~EVT-start = DVT阶段，以此类推
  - MP日期之后不再画
  - na的Stage直接跳过，不画
  - MVT用小写m，MP用大写M
